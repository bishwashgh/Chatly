{$DOMAIN} {
	encode gzip zstd

	@media path /media/*
	handle @media {
		reverse_proxy {$BACKEND_ADDR}
	}

	handle {
		reverse_proxy {$BACKEND_ADDR}
	}

	header {
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
	}
}

# WebSocket upgrade is automatic with reverse_proxy in Caddy 2